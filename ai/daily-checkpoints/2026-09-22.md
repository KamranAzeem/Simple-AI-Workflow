<!-- Daily checkpoint (diary). Append-only: one file per calendar day, newest section at the bottom. Never edit or reorder earlier sections. This is the only archive. -->

# Daily Checkpoint 2026-09-22

## Full-history purge of sensitive identifiers, tags recreated, global commit guardrail

- Purged sensitive identifiers (client project, person names, a company name, client ticket prefixes, and client infrastructure names) from the entire git history with `git filter-repo --replace-text --replace-message --mailmap`, from the first affected commit `888bdf6` (2026-06-22) forward. All now read `[redacted]`.
- Scan results before the purge: no credentials, private keys, tokens, or connection strings anywhere; `ai/secrets/` was never committed. Identifiers lived only in markdown prose, two commit messages, and three tags.
- Normalized all 223 commit identities to `kamranazeem@gmail.com`.
- Recreated and force-pushed all five tags (`v1.0.0`..`v2.3.0`) with their original messages, and force-pushed `master`. The `v2.4.0` release tag is deferred until the README overhaul merges.
- Verification: every old commit maps to a new one (221 old, zero missing); old and new master file lists identical; commit counts preserved; tag messages byte-identical; validator 8/8; markdownlint 0; a fresh mirror clone has zero identifier hits across all 223 commits.
- Removed the two stale files in `ai/artifacts/` (backed up to `/tmp/kilo/artifacts-removed/`) and added `ai/artifacts/` to `.gitignore`.
- Moved the authoritative sensitive-name list into `~/.ai/settings/global-user-settings.md` with a pre-commit scan rule; genericized the same identifiers in `~/.ai/global-knowledge/engineering-lessons-and-conventions.md`.
- Plan `ai/plans/full-history-sensitive-data-purge-plan.md`; peer review `ai/code-review-reports/2026-09-22_00-16_review-18.md` APPROVED. ADR recorded in `ai/shared/project-knowledge/protocol-decisions.md`.
- Branch `docs/readme-overhaul` was rewritten locally and stays unmerged. Its tracking artifacts (state files, checkpoints 2026-09-20/21, the nine tickets, notes, sync-scripts-testing) were reconciled onto `master`.
- README migration notice added: existing clones must run `git fetch origin && git reset --hard origin/master`.

## Documentation cleanup merged to master, tickets closed, branch deleted

- Squash-merged `docs/readme-overhaul` into `master` as `ac516b3` (`docs: overhaul README, sync docs with post-v2.3.0 features, and prep release`), 42 files changed. Not pushed.
- Closed three tickets as the final branch change, per the close-before-merge rule: `common-instructions-apply-fixes`, `readme-progressive-disclosure-note`, `documentation-cleanup-overhaul`. `ai/issues/in-progress/` is now empty.
- Deleted the local branch `docs/readme-overhaul` after confirming its tree matched `master`.
- Synced the slides and workflow guide with post-v2.3.0 features; removed stale files (`tools-preferences.md`, the executed purge plan, the self-consistency LLD, the sync-scripts testing record); renamed `docs/about-human.md` to `docs/global-user-settings.md`.
- Checkpoint: trimmed two old `progress.md` entries already mirrored in the 2026-09-17 diary; dashboard now shows 24 open, 0 in-progress, 13 closed.
- Release `v2.4.0` remains on hold pending the bounded boot-time staleness heuristic.
- Validation: validator v5.0 8/8, markdownlint 0 on tracked markdown.

## Bounded boot-time staleness heuristic implemented; releasing v3.0.0

- Implemented the bounded boot-time staleness heuristic on branch `feature/bounded-staleness-heuristic`: metadata-only age for Project Knowledge at boot, a Proof-of-Load bullet, a policy rule, and docs sync (workflow guide, slides, README).
- Plan peer-reviewed: review-19 CHANGES REQUESTED, review-20 APPROVED. Implementation peer-reviewed: review-21 CHANGES REQUESTED (README sync), review-22 APPROVED.
- Protocol Developer Mode: `protocol-decisions.md` loaded in full before the protocol edits. The rule is freshness-only and does not re-introduce the rejected broad distrust clause.
- Next: close the ticket, squash-merge to `master`, tag `v3.0.0` (MAJOR, history rewrite), and publish the GitHub release.

### v3.0.0 released

- Closed the ticket on the branch, squash-merged as `df3ffef`, deleted the branch, and pushed `master` (`335824f..df3ffef`).
- Tagged `v3.0.0` and published the GitHub release (marked Latest) with notes for the breaking history rewrite, features, privacy, and docs.
- State: 23 open, 0 in-progress, 14 closed. Validator 8/8, markdownlint 0.

### Session wrap

- Ended the session after the v3.0.0 release. State files are current; no protocol or code changes after the release checkpoint.
- Drafted a short markdown update note for users and close friends, with the history-rewrite pull steps. It is not a repo artifact.
- Next session: verify a fresh clone on another machine, then work the 23 open tickets.

## No pre-work commits policy shipped

- Removed the Branch-Gating exception in `ai-policy-common.md` and added the No pre-work commits rule: every commit carries the work; when a work item goes on a branch, open the branch before the first commit.
- Added a Required Pre-action Check to `ai-policy-meta.md`: every protocol or policy change adds a dated `protocol-decisions.md` entry.
- Recorded the ADR in `protocol-decisions.md`, synced `docs/workflow-guide.md` section 4, closed the `pre-work-commits-on-master` ticket on the branch, and removed the executed plan.
- Plan and implementation peer-reviewed: review-01 CHANGES REQUESTED, review-02 APPROVED; implementation review-03 CHANGES REQUESTED, review-04 and review-05 APPROVED. The user approved a conditional wording edit for the rule's last sentence.
- Squash-merged to `master`, branch deleted, pushed to origin. Validator v5.0 8/8; markdownlint 0.
- Scoped out: the cross-machine state-commit cycle (hash half resolved by State-File Model v2; persistence half inherent) and housekeeping-commit batching.
- State: 23 open, 0 in-progress, 15 closed.

## Executed bounded-staleness plan removed and progress budget trimmed

- Removed `ai/plans/bounded-boot-time-staleness-heuristic-plan.md`, the executed v3.0.0 plan; its decisions live in `protocol-decisions.md`. The only remaining reference is the historical Files changed list in the ADR entry.
- Trimmed the oldest `progress.md` entry (the 2026-09-18 Horizon Shield) to the diary: the file was 67 bytes over the 7 KB budget, and the entry is mirrored verbatim in `ai/daily-checkpoints/2026-09-18.md`.
- No new work otherwise. State: 23 open, 0 in-progress, 15 closed. Validator v5.0 8/8; markdownlint clean.
