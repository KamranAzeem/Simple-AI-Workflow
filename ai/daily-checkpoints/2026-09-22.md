<!-- Daily checkpoint (diary). Append-only: one file per calendar day, newest section at the bottom. Never edit or reorder earlier sections. This is the only archive. -->

# Daily Checkpoint 2026-09-22

## Full-history purge of sensitive identifiers, tags recreated, global commit guardrail

- Purged sensitive identifiers (client project, person names, a company name, client ticket prefixes, and client infrastructure names) from the entire git history with `git filter-repo --replace-text --replace-message --mailmap`, from the first affected commit `888bdf6` (2026-06-22) forward. All now read `[redacted]`.
- Scan results before the purge: no credentials, private keys, tokens, or connection strings anywhere; `ai/secrets/` was never committed. Identifiers lived only in markdown prose, two commit messages, and three tags.
- Normalized all 223 commit identities to `kamranazeem@gmail.com`.
- Recreated and force-pushed all five tags (`v1.0.0`..`v2.3.0`) with their original messages, and force-pushed `master`. New master tip `5ffb857`. Tag `v2.4.0` marks the purge release.
- Verification: every old commit maps to a new one (221 old, zero missing); old and new master file lists identical; commit counts preserved; tag messages byte-identical; validator 8/8; markdownlint 0; a fresh mirror clone has zero identifier hits across all 223 commits.
- Removed the two stale files in `ai/artifacts/` (backed up to `/tmp/kilo/artifacts-removed/`) and added `ai/artifacts/` to `.gitignore`.
- Moved the authoritative sensitive-name list into `~/.ai/settings/global-user-settings.md` with a pre-commit scan rule; genericized the same identifiers in `~/.ai/global-knowledge/engineering-lessons-and-conventions.md`.
- Plan `ai/plans/full-history-sensitive-data-purge-plan.md`; peer review `ai/code-review-reports/2026-09-22_00-16_review-18.md` APPROVED. ADR recorded in `ai/shared/project-knowledge/protocol-decisions.md`.
- Branch `docs/readme-overhaul` was rewritten locally and stays unmerged. Its tracking artifacts (state files, checkpoints 2026-09-20/21, the nine tickets, notes, sync-scripts-testing) were reconciled onto `master`.
- README migration notice added: existing clones must run `git fetch origin && git reset --hard origin/master`.
