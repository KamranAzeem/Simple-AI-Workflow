# Daily Checkpoint 2026-09-09

## CP-2026-09-09-01: Fixed checkpoint procedure never writing the daily checkpoint file

**Problem**: Procedure C mandated writing the three **Project AI State Files** but never wrote a file in **Project Daily Checkpoints Directory** — only Procedure B (bootstrap, one-time) ever created one. Meanwhile `ai-policy-common.md`'s Source-of-Truth Order and State File Proof-of-Read rule both already assumed a current checkpoint file existed. Result: state files raced ahead with new CP IDs every checkpoint while `ai/daily-checkpoints/` only advanced when an AI session happened to write one unprompted. Reported from an elmera-project session (`ai/issues/checkpoint-procedure-never-writes-daily-checkpoint-file.md`) and confirmed live in this repo: state files were already at CP-2026-09-07-02 while the latest file on disk was `2026-09-04.md`.

**Fix**: Added a new step 2, **Write Daily Checkpoint File**, to Procedure C in `AGENTS.md`, between the Atomic Write Protocol (step 1) and Log Condensation (renumbered 2→3; Update Project Knowledge 3→4; Context Re-affirmation 4→5). One internal cross-reference to the old step 2 (the horizon-shield mention) was corrected to "(Step 3)".

**Naming convention — deliberate deviation from the issue's raw proposal**: the issue's proposed fix cited an elmera-project example using `YYYY-MM-DD-NN.md` (one file per checkpoint). Investigated this repo's own `ai/daily-checkpoints/` directory instead and found the convention actually in force since 2026-06-19 is one file per **calendar day** (`YYYY-MM-DD.md`) with multiple `## CP-<ID>: <title>` sections appended for same-day checkpoints (verified via `2026-08-25.md`, which holds four checkpoint sections). Used that convention rather than importing an example from a different project that was never actually the standard here.

**Files changed**:
- `AGENTS.md` — Procedure C, new step 2 + renumbering + cross-ref fix
- `ai/policies/ai-policy-common.md` — new "Daily Checkpoint File Mandate" bullet (no step numbers, per the 2026-08-05 policy-files rule)
- `support-files/validate-protocol.sh` — v4.6 → v4.7, new anchor check for "Write Daily Checkpoint File"
- `docs/workflow-guide.md` — new "Daily Checkpoint File" subsection in §14
- `docs/simple-ai-workflow-slides.md` — matching bullet on the "atomic writes and a sliding horizon" slide
- `ai/issues/checkpoint-procedure-never-writes-daily-checkpoint-file.md` — Status updated to Resolved
- `ai/shared/project-knowledge/protocol-decisions.md` — new dated entry with full rationale
- `ai/artifacts/` — created (pre-existing missing mandatory directory, unrelated to this fix, was blocking the validator)

**Validation**: `support-files/validate-protocol.sh` run twice (fail on missing `ai/artifacts/`, then pass 8/8 after creating it). Peer review (Procedure D) run proactively per the standing rule in `ai-customization.md` — `ai/code-review-reports/2026-09-09_15-30_review-01.md`, verdict **APPROVED**, no Critical/Major/Minor findings.

**Git-drift check (requested before commit)**: `git fetch --all --prune` + `git rev-parse HEAD`/`origin/master` confirmed both at `16899ea`, no divergence in either direction. Root cause of the previously flagged "drift": `context.md`'s own entry for CP-2026-09-07-02 said "Not committed", but `git show --stat 16899ea` proved that checkpoint's commit (titled "checkpoint CP-2026-09-07-02") was already made and pushed — a stale self-report, not an actual local/remote mismatch. Corrected `context.md`'s CP-2026-09-07-02 entry and Current Status accordingly; removed the now-resolved next-steps item.

**Status**: Committed and pushed as `f716105` (correction: this entry originally said "Not committed, pending user review"; the user reviewed and approved the commit in this same session — updated to reflect that outcome).

## CP-2026-09-09-02: Formalized ai/issues/ in the protocol (Project Issues Directory)

**Problem**: `ai/issues/` had been used informally (starting with the CP-01 issue above) but had no defined place in the protocol: not in TIER 1, not indexed by Procedure A, not surfaced in the Proof-of-Load report. See `ai/issues/closed-proof-of-load-report-should-index-issues-directory.md` for the original report.

**Fix**: Added **Project Issues Directory** (`ai/issues/`) to `AGENTS.md` TIER 1, alphabetically between Handoffs and AI Knowledge Directory — scoped per-project (not global; issues can be about the protocol or any sibling project under the same root, corrected from the issue's own "global-only" guess). Procedure A Step 2 (Structural Audit) now lists it as mandatory; Step 5 (Knowledge Loading) indexes it by filename + line count, excluding files prefixed `closed-`; Step 7 (Proof-of-Load report) gained bullet (g): open issue count + filenames + line counts.

**Lifecycle decision**: considered `ai/issues/open/`+`ai/issues/closed/` subdirectories and a `## Status` content field; rejected both in favor of a flat directory with a `closed-` filename prefix. A content field requires opening every file to know its state (violates Token Rationing, reintroduces the exact text-drift risk CP-01 just fixed); subdirectories add an unnecessary directory-existence question. Closing an issue = `git mv` to prefix the filename with `closed-`, in place; reopening strips the prefix.

**Files changed**:
- `AGENTS.md` — TIER 1 entry; Procedure A Steps 2, 5, 7
- `support-files/validate-protocol.sh` — v4.7 → v4.8: `Project Issues Directory` added to CONFIG_KEYS, `issues` added to PROJECT_SUBS, new anchor check for the `closed-` convention text
- `ai/shared/project-knowledge/protocol-decisions.md` — new dated entry with full rationale
- `ai/issues/checkpoint-procedure-never-writes-daily-checkpoint-file.md` → renamed to `closed-checkpoint-procedure-never-writes-daily-checkpoint-file.md`
- `ai/issues/proof-of-load-report-should-index-issues-directory.md` → renamed to `closed-proof-of-load-report-should-index-issues-directory.md`, Status updated to Resolved

**Validation**: `support-files/validate-protocol.sh` run 3 times across the edit sequence, 8/8 pass each time (final run at v4.8). Peer review (Procedure D) — `ai/code-review-reports/2026-09-09_16-17_review-02.md`, verdict **APPROVED**, no Critical/Major/Minor findings (two non-blocking suggestions noted).

**Status**: Not committed. On branch `feature/proof-of-load-issues-directory-indexing`. Pending user review and explicit approval before commit/merge, per Non-Negotiable #6.
