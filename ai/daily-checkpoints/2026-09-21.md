<!-- Daily checkpoint (diary). Append-only: one file per calendar day, newest section at the bottom. Never edit or reorder earlier sections. This is the only archive. -->

# Daily Checkpoint 2026-09-21

## README overhaul and global user settings rename

- Renamed `docs/about-human.md` to `docs/global-user-settings.md`, fixed its header comment, the runbook reference, and the `.gitignore` comment, and rewrote it as an OS-agnostic template: identity placeholders, and a CLI tool list that starts as names only and records each path after install. `Communication Style` is now a subsection of `Preferences and Style`, not a duplicate.
- Overhauled `README.md` on branch `docs/readme-overhaul`: merged the two setup flows, removed the invalid step that set the workflow directory in `AGENTS.md` (it is set in `ai-customization.md`), fixed the "How it works" diagram, completed the docs list, added an Examples section, and added `"perform a code review on <topic>"` and `"apply fixes for problems identified in code review report"` to Common instructions.
- Peer reviews: review-15 CHANGES REQUESTED (the diagram implied the repo's `ai/` is copied; setup copy count; missing `~/.ai/backups/`; post-compaction wording), fixes applied, review-16 APPROVED. One finding was withdrawn as unsupported (the customization list).
- Ticket `common-instructions-apply-fixes.md` moved to `in-progress`; the rest of the README work belongs to `documentation-cleanup-overhaul.md`.
- Issue counts 26 open / 1 in-progress / 10 closed. Validator v5.0 8/8; markdownlint 0 issues.
- Branch `docs/readme-overhaul` is not merged and nothing is committed yet. The branch also carries the earlier uncommitted tracking work (state files, the 2026-09-20 diary, the new issues, `sync-scripts-testing.md`).
