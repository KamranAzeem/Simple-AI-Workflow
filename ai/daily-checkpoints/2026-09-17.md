# Daily Checkpoint 2026-09-17

## CP-2026-09-17-01: Issue-management mechanism implemented (status by directory)

**Problem**: The 2026-09-09 issue formalization used a flat `ai/issues/` directory with a `closed-` filename prefix for status. In use the prefix became visual noise on every ticket, and the three-state lifecycle had no home for `in-progress` without a second prefix and a second rename. A 2026-09-17 observation on the implementation issue proposed moving status into the directory structure.

**Fix**: Implemented the mechanism with status as location. Tickets now live in `ai/issues/{open,in-progress,closed}/`; the filename is the slug only and never changes; moving between states is a directory move. Added `PROCEDURE H (Issue Management)` to `AGENTS.md` TIER 3, an Issue Management Protocol section to `ai-policy-common.md`, and the full ticket template at `ai/shared/project-knowledge/issue-template.md`. Procedure A Step 2 now runs one idempotent `mkdir -p` over the fixed mandatory-directory list (extended with the three status directories) and creates the template when missing, with the read-only Safety Barrier amended to permit exactly those creates. Proof-of-Load indexes `open/` and `in-progress/` by filename and line count. Validator v4.8 to v4.9. Six tickets were moved and given template headers; the two tickets this branch resolves sit in `in-progress/` until merge. Two prior decisions were reversed and recorded: the 2026-09-09 subdirectory rejection and the 2026-07-04 report-only rule.

**Horizontal-shield deferral**: `progress.md` (52 items) and `context.md` (16 entries) are both past their Horizon Shield thresholds. Archiving was deliberately deferred for explicit user confirmation, per the Bloat and Order Check rule. Flagged in `next-steps.md` and `context.md`.

**Files changed**: `AGENTS.md`; `ai/policies/ai-policy-common.md`; `ai/shared/project-knowledge/issue-template.md` (new); `support-files/validate-protocol.sh`; the six tickets under `ai/issues/` plus `ai/issues/in-progress/.gitkeep` (new); `ai/notes/issue-management-mechanism-design.md`; `ai/notes/grilling-procedure-design-note.md` and `ai/notes/agent-document-review-procedure-design-note.md` (renamed); `ai/notes/notes.md`; `ai/plans/issue-management-mechanism-implementation-plan.md` (new); `ai/shared/project-knowledge/protocol-decisions.md`; `.markdownlint-cli2.jsonc`; `ai/state/progress.md`, `ai/state/next-steps.md`, `ai/state/context.md`; this file.

**Validation**: `support-files/validate-protocol.sh` v4.9, 8/8. `markdownlint-cli2` 0 issues on all changed markdown. `bash -n` passed on the validator; `shellcheck` not installed. Peer review: plan review-01 CHANGES REQUESTED (five Major, all resolved), review-02 APPROVED, implementation review-03 APPROVED.

**Status**: Uncommitted on `feature/issue-management`. Awaiting user approval to commit and merge.

## CP-2026-09-17-02: Merged to master (feature/issue-management) and tickets closed

**Problem**: The mechanism was implemented on `feature/issue-management` but uncommitted, and its two tickets sat in `in-progress/` because the close rule says a ticket reaches `closed/` only when its fix is merged.

**Fix**: Squash-merged to `master` as `be4e4b1` (`feat(protocol): directory-based issue management (Procedure H)`) and pushed to `origin/master` (`9eba9f0..be4e4b1`). Deleted the local feature branch. Moved both resolved tickets to `ai/issues/closed/` and appended close updates, dogfooding the new lifecycle. Added a merge record to `protocol-decisions.md`.

**Files changed**: `ai/issues/closed/issue-management-mechanism.md`, `ai/issues/closed/boot-up-should-create-required-ai-directories.md`, `ai/shared/project-knowledge/protocol-decisions.md`, `ai/state/progress.md`, `ai/state/next-steps.md`, `ai/state/context.md`, this file.

**Validation**: `support-files/validate-protocol.sh` v4.9 8/8; markdownlint clean on the changed files.

**Status**: Pending the housekeeping commit and push.
