<!-- State file (future). Open items only, oldest first, delete on done. Short bullets. Never drop an unfinished item. -->
## Pending
- [ ] Implement the bounded boot-time staleness heuristic (`ai/issues/open/bounded-boot-time-staleness-heuristic.md`); release is on hold until it is done.
- [ ] Verify one fresh clone on another machine after the 2026-09-22 history rewrite; existing clones need `git fetch && git reset --hard`.
- [ ] After the staleness work: tag `v2.4.0` on `master` and write the GitHub release notes.

## Deferred
- [ ] Nothing deferred outside the issue tracker.
