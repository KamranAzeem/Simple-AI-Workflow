# Centralized Policy Management System for smarter AI workflow

- The user will copy the AGENTS.md in the project root.
- Update the policy URL
- May have a local policy overrite file for local adjustments. (ai/ai-policy-override.md)
- Will have local ai directory with session tracking and context tracking information.
- ai/ directory will be git ignored.
- Todo: Should the context.md be outside the ai directory as policy is already outside the project directory? Should there be a local presence of the policy file and check if the internet is available then the policy file will be freshly pulled.
- The system should honor the local policy file, if the centralized policy file is inaccessible (no internet).


